# Real-engine validation record

**Run date:** 2026-09-26 (Asia/Kuching)

## 1. Current runnable Godot Web deliverable

`build/web/` is generated by the GitHub Actions Web export job and is committed back to the active preview branch by that job so a plain local static server can open the actual engine build.

| Check | Result |
| --- | --- |
| Exporter | Godot `4.3.stable.official.77dcf97d8` with matching Web templates on GitHub Actions |
| Export mode | Web Release, Compatibility / WebGL 2, single-threaded, Keep-canvas policy |
| Payloads | `index.html`, `phagos.js`, `phagos.wasm`, `phagos.pck`, plus Brotli `.br` sidecars |
| Pack check | `phagos.pck` has a `GDPC` header |
| Loader | Custom immune-core loading screen is included in the exported `index.html` |

The successful official export was produced in GitHub Actions run `36197431446`. Its generated preview commit is `716ae84` (`chore(web): refresh generated Godot preview [skip ci]`).

## 2. Actual browser playback

The generated official build was served locally from this repository and run in a real WebGL browser session.

- Browser: npm-unpacked Chromium 153 with WebGL 2 / SwiftShader.
- URL: `http://127.0.0.1:8008/build/web/`.
- Engine console: `Godot Engine v4.3.stable.official.77dcf97d8`.
- Renderer console: `OpenGL ES 3.0 (WebGL 2.0) - Compatibility`.
- The custom loader dismissed and the Heart arena rendered.
- Canvas backing store and client size both measured **1920 × 1080**.
- Direct build navigation recorded **no console errors, page errors, or failed requests**.

This is an official Godot 4.3 Web runtime and PCK pair, not the earlier cross-minor diagnostic harness.

## 3. CI and browser-matrix status

- The CI screenshot stage completed successfully for Heart, Lung, Brain, and Bone Marrow at 1920 × 1080.
- The export archive (`web-preview.zip`) and quality-report artifact are uploaded before later QA gates, so a runnable build is preserved even if a browser gate fails.
- Chromium playback above is independently verified locally.
- The current strict Chrome / Firefox / Edge workflow gate still reports a failure and therefore needs its generated `browser_validation.md` reviewed before it can be called a full three-browser pass. Firefox and Edge are not inferred from Chromium.
- GitHub Pages is not enabled for this repository yet. Repository administrators must select **Settings → Pages → Source: GitHub Actions** once; the automation token cannot change that repository setting.

## 4. Asset and debug notes

The asset-manifest validator runs successfully, but the repository currently contains 98 missing optional source PNG specs. The manifest-backed procedural fallback is therefore the visual source of the validated arena frames.

The F3 performance overlay is intentionally debug-only (`OS.is_debug_build()`), so it is absent from the Web Release preview.
